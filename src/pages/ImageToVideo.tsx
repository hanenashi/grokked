import VideoGeneration from "./VideoGeneration";

/** Compatibility entry point retained for direct imports from the upstream app. */
export default function ImageToVideo() {
  return <VideoGeneration mode="image" />;
}
